require 'rails_helper'

RSpec.describe Post, type: :model do
  #pending "add some examples to (or delete) #{__FILE__}"
  # before(:each) { @user = User.new(email: 'user@example.com') }
  # before(:each) {@post = Post.new(content: 'Sample content for users post')}
  #   subject { @post }
  #   it "checks if content is present" do
  #     expect(@post.content).to match('Sample content for users post')
  #   end
  #   it "error if content is blank" do
  #     @post.content=''
  #     @post.save
  #     expect(@post.errors.messages[:content]).to include ("can't be blank")
  #   end

end
