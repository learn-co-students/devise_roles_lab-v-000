<h3>New Post by <%= current_user.email %></h3>
<%= form_for :post, :url => user_posts_path(current_user) do |f| %>
<%= f.label :content %>
<%= f.text_field :content %>
<%= f.hidden_field :owner_id, :value => current_user.id  %>
<%= f.submit %>
<% end %>
