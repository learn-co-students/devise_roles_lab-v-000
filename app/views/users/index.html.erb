<h1>List of Users</h1>
<% @users.each do |user| %>
  <h2><%= user.name %></h2>
  <h3><%= user.email %></h3>
  <%= form_for(user) do |f| %>
    <%= f.select(:role, User.roles.keys.map {|role| [role.titleize,role]}) %>
    <%= f.submit 'Change Role', :class => 'button-xs' %>
  <% end %>
  <% if current_user.admin? %>
    <%= link_to "Delete", user_path(user), method: :delete %> <br>
  <% end %>
<% end %>
