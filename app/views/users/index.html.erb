<h1>Users</h1>
<ul>
   <% @users.each do |user| %>
    <li><%= link_to user.name, user_path(user) %> <%= user.email if user == current_user%> <%= link_to "(edit)", edit_user_path(user)%> </li>
    <% end %>
</ul>